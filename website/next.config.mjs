/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['images.unsplash.com','res.cloudinary.com','gateway.pinata.cloud','aquamarine-urban-firefly-16.mypinata.cloud'],
  },
};

export default nextConfig;
